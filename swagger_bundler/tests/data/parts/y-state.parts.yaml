x-bundler-compose:
  - type.parts.yaml

definitions:
  state:
    properties:
      type:
        $ref: "#/definitions/type"
      status:
        type: string
        enum:
          - ok
          - ng

